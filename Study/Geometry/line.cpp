#include <bits/stdc++.h>

using namespace std;

struct line {
	double a, b, c;
	line() { a = b = c = 0.0; }
	line(double _a, double _b, double _c) : a(_a), b(_b), c(_c) {}
};

int main () {

	

	return 0;
}
