#include <bits/stdc++.h>
#define maxn 1001

using namespace std;

int v[maxn];

int main () {
	int k, n; scanf("%d %d", &k, &n);

	for (int i = 0; i < n; i++) {
		scanf("%d", v+i);
	}

	map<int, int> mapa;
	
	for (int i = 0; i < k; i++) {

	}

	return 0;
}
